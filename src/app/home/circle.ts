export class Circle {
    radius: number;
    x: number;
    y: number;
    fill: string;

    constructor(radius: number, x: number, y: number, fill: string){
        this.radius = radius;
        this.x = x;
        this.y = y;
        this.fill = fill;
    }
}