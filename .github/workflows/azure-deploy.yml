on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 'Deploy to Azure'
      run: |
        sudo npm install -g @angular/cli
        npm install
        ng run cv:deploy
